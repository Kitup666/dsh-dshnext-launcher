# 醒来看：功能检查结果与待决定事项

2026-09-07 夜间做了全量功能检查（测试 27/27、e2e 全链路、六页出图 judge 验收 6/6、
真机双开验证）。发现并修好一个问题，另有几件事需要你拍板。

## 已修好（不需要你做什么）

### 手动搬迁的遗留伤害：harness 起不来（已修复 ✅）

`--e2e` 验证时发现 harness 启动即退。根因不是启动器代码：你当时**手动**把目录从
C 搬到 D 时，搬运方式把 dsh 托管的 module proxy（`home/profiles/node_modules` 下
250MB）解引用成了普通目录拷贝。dsh 启动自检发现「该是托管代理的位置躺着一堆实体
目录」直接拒绝运行。

处理：删掉这份陈旧副本（它是 runtime 官方 bundle 的完整重复，无独有数据），让 dsh
自己自愈重建了 483 个条目。之后 harness 正常启动（WebUI 在 3080 监听），启动器
`--e2e` 全链路（启动 → 运行 8 秒 → 停止）复验通过。

顺带：启动器的目录迁移代码（两阶段 + junction 重建）就是防这个的——下次再用界面
里的「修改目录」不会重演。

## 需要你行动的

### 1. API key 重填 ⚠️

`D:\Codeagent\dsh\config.json` 里的 `api_key` 在迁移乱局中丢了（C 盘旧目录已空，
没有找到备份）。启动 harness 前要在设置页重填一次 DeepSeek key。

## 需要你拍板的

### 2. 旧预设（~/.dsh）要不要合并进来

`C:\Users\24453\.dsh\` 是你 8 月以来终端直接跑 dsh 攒下的家目录，预设（hmode、
kmode、router-* 等）都在那里。启动器实例用的是 `D:\Codeagent\dsh\home`，两边互相
看不见。**选项**：a) 我把 `.agent-presets`（和 settings.yaml 挑有价值的）复制进
启动器 home；b) 保持分开（终端跑的归终端，启动器归启动器）；c) 把启动器 dsh-home
直接改指 ~/.dsh（不推荐，会完全混在一起）。

### 3. NSIS 安装包重编

一直挂着等视觉验收。今晚 judge 六页 6/6 全过，可以打包了——打包后要做真装真卸
一轮验证（会真装到你机器上再卸掉）。**要我做就说一声，或直接批准我做。**

### 4. 并行跑多个 profile 要不要支持

目前全启动器共享一个端口（config.port），默认同一时间只能跑一个 profile。要支持
并行需要：per-profile 端口 + `settings.yaml`/`storages`/`.credentials.yaml` 这些
共享文件的隔离方案。是个不小的改动，**值不值得做你定**。

### 5. 运行中升级 dsh 的体验

今晚给「装/卸 dsh、Node、pnpm」加了运行中禁止的守卫（防止实例吃到半新半旧的模块）。
代价是：实例跑着的时候不能升级 dsh，要先停。**可选改进**：守卫提示里加「一键停止
并继续升级」。要的话我加。

### 6. 实例运行中装/卸插件目前不拦

插件操作只动 profile 目录，不动 runtime，所以没加守卫。dsh 配置里有
`patchReload: live`，疑似支持热载。**未验证热载行为**——如果你希望运行中也先停
再装插件，说一声，我加同样的守卫。

---
*今晚的代码提交：cd0984c（迁移重写）→ 833e9d5（进度条）→ 5e68d71（守卫+单实例）
→ 42d3b31。全部已推送至本地 master，工作区干净。*
