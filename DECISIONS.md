# 醒来看：功能检查结果与待决定事项

2026-09-07 夜间做了全量功能检查（测试 27/27、e2e 全链路、六页出图 judge 验收 6/6、
真机双开验证）。发现并修好一个问题，另有几件事需要你拍板。

## 已修好（不需要你做什么）

### 手动搬迁的遗留伤害：harness 起不来（已修复 ✅）

`--e2e` 验证时发现 harness 启动即退。根因不是启动器代码：你当时**手动**把目录从
C 搬到 D 时，搬运方式把 dsh 托管的 module proxy（`home/profiles/node_modules` 下
250MB）解引用成了普通目录拷贝。dsh 启动自检发现「该是托管代理的位置躺着一堆实体
目录」直接拒绝运行。

处理：删掉这份陈旧副本（它是 runtime 官方 bundle 的完整重复，无独有数据），让 dsh
自己自愈重建了 483 个条目。之后 harness 正常启动（WebUI 在 3080 监听），启动器
`--e2e` 全链路（启动 → 运行 8 秒 → 停止）复验通过。

顺带：启动器的目录迁移代码（两阶段 + junction 重建）就是防这个的——下次再用界面
里的「修改目录」不会重演。

## 需要你行动的

### 1. API key 重填 ⚠️

`D:\Codeagent\dsh\config.json` 里的 `api_key` 在迁移乱局中丢了（C 盘旧目录已空，
没有找到备份）。启动 harness 前要在设置页重填一次 DeepSeek key。

## 需要你拍板的

### 2. 旧预设（~/.dsh）要不要合并进来

**已决定（2026-09-07 晨）：不合并，用户自己看情况搬。** 启动器 home 与
`C:\Users\24453\.dsh\` 保持分开。

### 3. NSIS 安装包重编

一直挂着等视觉验收。今晚 judge 六页 6/6 全过，可以打包了——打包后要做真装真卸
一轮验证（会真装到你机器上再卸掉）。**要我做就说一声，或直接批准我做。**

### 4. 并行跑多个 profile 要不要支持

**已决定（2026-09-07 晨）：不做。** 正常只让开一个（单实例互斥已落地，端口全局
一个，行为符合预期）。

### 5. 运行中升级 dsh 的体验

**已做（2026-09-07 晨，commit 3e0300f）：** 守卫从干拒绝升级为「停止并继续」
确认框——确认后自动停全部实例，停干净自动续跑原操作。

### 6. 实例运行中装/卸插件目前不拦

**已决定（2026-09-07 晨）：保持不拦。** dsh 本身支持热载（patchReload: live），
插件操作只动 profile 目录不碰 runtime。

---
*今晚的代码提交：cd0984c（迁移重写）→ 833e9d5（进度条）→ 5e68d71（守卫+单实例）
→ 42d3b31 → 3e0300f（停止并继续）。全部已推送至本地 master，工作区干净。*
